{
  "private": true,
  "scripts": {
    "watch": "node esbuild.config.mjs",
    "build": "tsc -noEmit && node esbuild.config.mjs build"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "@tailwindcss/forms": "^0",
    "@types/chrome": "^0",
    "daisyui": "^5",
    "esbuild": "^0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
