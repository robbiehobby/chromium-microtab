{
  "private": true,
  "scripts": {
    "watch": "node esbuild.config.mjs",
    "build": "tsc -noEmit && node esbuild.config.mjs build"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0",
    "@types/chrome": "^0",
    "daisyui": "^4.12.23",
    "esbuild": "^0",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.3"
  }
}
